# Production environment — copy to .env.production locally or set in CI/Vercel
# NEVER commit .env.production with real secrets

VITE_APP_VERSION=1.0.0
VITE_ENV=production

# Sentry DSN for production
VITE_SENTRY_DSN=your-production-sentry-dsn

# GA4 for production
VITE_GA4_ID=your-production-ga4-id

# Optional: map provider (defaults to Yandex if unset)
# VITE_MAP_PROVIDER_BASE=
# VITE_IMAGE_CDN_BASE=
